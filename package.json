{
  "name": "invoice-maker-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd invoice-react && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "googleapis": "^170.1.0",
    "mongoose": "^8.21.1",
    "nodemailer": "^7.0.12"
  },
  "engines": {
    "node": "20.x"
  }
}
